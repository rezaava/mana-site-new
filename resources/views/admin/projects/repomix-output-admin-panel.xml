This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
create.blade.php
edit.blade.php
index.blade.php
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="create.blade.php">
@extends('admin.panel')

@section('content')
<div style="padding:20px;">
    <div style="background:var(--card-bg);border-radius:12px;padding:20px;">
        <h5 style="margin-bottom:20px;">
            <i class="fa-solid fa-plus-circle"></i> افزودن پروژه جدید
        </h5>

        <form action="{{ route('projects.store') }}" method="POST" enctype="multipart/form-data">
            @csrf

            {{-- ===== اطلاعات اصلی پروژه ===== --}}
            <div class="row g-3 mb-4">
                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">عنوان پروژه</label>
                    <input type="text" name="title" value="{{ old('title') }}" required class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">زیرعنوان</label>
                    <input type="text" name="subtitle" value="{{ old('subtitle') }}" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">دسته‌بندی</label>
                    <select name="cat_id" class="form-select" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                        <option value="">انتخاب دسته‌بندی...</option>
                        @foreach($categories as $category)
                            <option value="{{ $category->id }}" {{ old('cat_id') == $category->id ? 'selected' : '' }}>{{ $category->name }}</option>
                        @endforeach
                    </select>
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">نام کارفرما</label>
                    <input type="text" name="client_name" value="{{ old('client_name') }}" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">سمت کارفرما</label>
                    <input type="text" name="client_role" value="{{ old('client_role') }}" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">سال اجرا</label>
                    <input type="text" name="launch_year" value="{{ old('launch_year') }}" placeholder="مثلاً ۱۴۰۲" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">مدت زمان</label>
                    <input type="text" name="duration" value="{{ old('duration') }}" placeholder="مثلاً ۳ سال" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;">لینک پروژه</label>
                    <input type="url" name="project_link" value="{{ old('project_link') }}" placeholder="https://..." class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>

                <div class="col-12">
                    <label style="display:block;margin-bottom:8px;">توضیح کوتاه (Brief)</label>
                    <textarea name="brief" rows="2" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">{{ old('brief') }}</textarea>
                </div>

                <div class="col-12">
                    <label style="display:block;margin-bottom:8px;">توضیحات کامل پروژه</label>
                    <textarea name="desc" rows="4" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">{{ old('desc') }}</textarea>
                </div>

                <div class="col-12">
                    <label style="display:block;margin-bottom:8px;">هدف پروژه</label>
                    <textarea name="project_goal" rows="3" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">{{ old('project_goal') }}</textarea>
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;color:#ffc107;"><i class="fa-solid fa-triangle-exclamation"></i> چالش اصلی</label>
                    <textarea name="challenge" rows="4" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">{{ old('challenge') }}</textarea>
                </div>

                <div class="col-md-6">
                    <label style="display:block;margin-bottom:8px;color:#0dcaf0;"><i class="fa-solid fa-lightbulb"></i> راه‌حل ما</label>
                    <textarea name="solution" rows="4" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">{{ old('solution') }}</textarea>
                </div>

                <div class="col-12">
                    <label style="display:block;margin-bottom:8px;">نقل قول از کارفرما (Testimonial)</label>
                    <textarea name="testimonial" rows="3" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">{{ old('testimonial') }}</textarea>
                </div>

                <div class="col-12">
                    <label style="display:block;margin-bottom:8px;">تصویر شاخص پروژه</label>
                    <input type="file" name="image" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                </div>
            </div>

            <hr style="border-color:var(--border);margin:30px 0;">

            {{-- ===== آمارها (Project Stats) ===== --}}
            <h6 style="margin-bottom:20px;color:var(--accent);">
                <i class="fa-solid fa-chart-simple"></i> آمارهای پروژه (حداکثر ۴ مورد)
            </h6>
            <div class="row g-3 mb-4" id="statsContainer">
                @for($i = 0; $i < 4; $i++)
                    <div class="col-md-3">
                        <input type="text" name="stats_value[]" placeholder="مقدار (مثلاً ۴۵%)" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;margin-bottom:6px;">
                        <input type="text" name="stats_label[]" placeholder="برچسب (مثلاً افزایش نرخ تبدیل)" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                    </div>
                @endfor
            </div>

            <hr style="border-color:var(--border);margin:30px 0;">

            {{-- ===== گالری تصاویر (Project Galleries) ===== --}}
            <h6 style="margin-bottom:20px;color:var(--accent);">
                <i class="fa-solid fa-images"></i> گالری تصاویر (هر دسته‌بندی ۳ تصویر)
            </h6>

            @php
                $categories = ['desktop' => 'دسکتاپ', 'mobile' => 'موبایل', 'key_pages' => 'صفحات کلیدی'];
            @endphp

            @foreach($categories as $catKey => $catLabel)
                <div class="mb-3">
                    <label style="display:block;margin-bottom:8px;font-weight:600;">{{ $catLabel }}</label>
                    <div class="row g-2">
                        @for($i = 0; $i < 3; $i++)
                            <div class="col-md-4">
                                <input type="file" name="gallery_images[{{ $catKey }}][]" class="form-control" style="width:100%;padding:6px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                            </div>
                        @endfor
                    </div>
                </div>
            @endforeach

            <hr style="border-color:var(--border);margin:30px 0;">

            {{-- ===== تکنولوژی‌ها (Project Technologies) ===== --}}
            <h6 style="margin-bottom:20px;color:var(--accent);">
                <i class="fa-solid fa-code"></i> تکنولوژی‌های استفاده‌شده
            </h6>
            <div class="row g-3 mb-4" id="techContainer">
                @for($i = 0; $i < 8; $i++)
                    <div class="col-md-3">
                        <input type="text" name="tech_name[]" placeholder="نام (مثلاً React)" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;margin-bottom:6px;">
                        <input type="text" name="tech_icon[]" placeholder="آیکون (مثلاً fa-brands fa-react)" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                    </div>
                @endfor
            </div>

            <hr style="border-color:var(--border);margin:30px 0;">

            {{-- ===== خدمات (Project Services) ===== --}}
            <h6 style="margin-bottom:20px;color:var(--accent);">
                <i class="fa-solid fa-list-check"></i> خدمات ارائه‌شده در پروژه
            </h6>
            <div class="row g-3 mb-4" id="servicesContainer">
                @for($i = 0; $i < 5; $i++)
                    <div class="col-md-3">
                        <input type="text" name="service_name[]" placeholder="نام خدمت (مثلاً طراحی UX/UI)" class="form-control" style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--border);background:transparent;color:inherit;">
                    </div>
                @endfor
            </div>

            <div style="margin-top:30px;">
                <button type="submit" class="btn btn-primary" style="padding:10px 30px;border-radius:8px;border:none;cursor:pointer;">
                    <i class="fa-solid fa-check"></i> ذخیره پروژه
                </button>
                <a href="{{ route('projects.index') }}" class="btn btn-secondary" style="padding:10px 30px;border-radius:8px;border:1px solid var(--border);text-decoration:none;">انصراف</a>
            </div>
        </form>
    </div>
</div>
@endsection
</file>

<file path="edit.blade.php">
@extends('admin.panel')

@section('content')
<style>
    /* استایل‌های اضافی برای فرم ویرایش */
    .form-section-title {
        font-size: 1.1rem;
        font-weight: 700;
        color: var(--accent);
        margin: 30px 0 20px;
        padding-bottom: 10px;
        border-bottom: 2px solid var(--border);
        display: flex;
        align-items: center;
        gap: 10px;
    }
    .form-section-title i {
        font-size: 1.2rem;
    }
    .form-group {
        margin-bottom: 1.2rem;
    }
    .form-label {
        display: block;
        font-weight: 600;
        margin-bottom: 6px;
        color: var(--text-dim);
    }
    .form-control-custom {
        width: 100%;
        padding: 10px 14px;
        border-radius: 8px;
        border: 1px solid var(--border);
        background: transparent;
        color: inherit;
        transition: border-color 0.3s;
    }
    .form-control-custom:focus {
        border-color: var(--accent-2);
        outline: none;
        box-shadow: 0 0 0 3px rgba(23, 195, 178, 0.15);
    }
    .form-control-custom::placeholder {
        color: var(--text-dimmer);
        opacity: 0.7;
    }
    .file-input-wrapper {
        position: relative;
        display: flex;
        align-items: center;
        gap: 10px;
        flex-wrap: wrap;
    }
    .file-input-wrapper input[type="file"] {
        flex: 1;
        padding: 8px;
        border: 1px dashed var(--border);
        border-radius: 8px;
        background: var(--bg-soft);
        color: var(--text-dim);
        cursor: pointer;
    }
    .file-input-wrapper input[type="file"]:hover {
        border-color: var(--accent-2);
    }
    .existing-image-preview {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        margin-top: 8px;
    }
    .existing-image-preview img {
        width: 70px;
        height: 70px;
        object-fit: cover;
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 3px;
        background: var(--bg);
    }
    .stats-grid, .tech-grid, .services-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 15px;
        margin-top: 10px;
    }
    .gallery-category {
        background: var(--bg-soft);
        border-radius: 12px;
        padding: 18px 20px;
        margin-bottom: 20px;
        border: 1px solid var(--line);
    }
    .gallery-category .category-label {
        font-weight: 700;
        color: var(--text);
        margin-bottom: 12px;
        display: block;
        font-size: 0.95rem;
    }
    .gallery-category .gallery-files {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 12px;
    }
    .gallery-category .gallery-files .file-item {
        display: flex;
        flex-direction: column;
        gap: 6px;
    }
    .gallery-category .gallery-files .file-item input[type="file"] {
        padding: 6px;
        border: 1px dashed var(--border);
        border-radius: 6px;
        background: var(--surface);
        font-size: 0.85rem;
        color: var(--text-dim);
    }
    .gallery-category .gallery-files .file-item .preview-img {
        width: 100%;
        height: 80px;
        object-fit: cover;
        border-radius: 6px;
        border: 1px solid var(--border);
        background: var(--bg);
    }
    .btn-secondary {
        background: var(--surface-2);
        color: var(--text);
        border: 1px solid var(--border);
        padding: 10px 25px;
        border-radius: 8px;
        text-decoration: none;
        display: inline-block;
        transition: all 0.3s;
    }
    .btn-secondary:hover {
        background: var(--surface);
        border-color: var(--accent-2);
    }
    .btn-primary {
        background: linear-gradient(95deg, var(--brand), var(--accent-2));
        color: var(--oncta);
        border: none;
        padding: 10px 30px;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 600;
        transition: all 0.3s;
    }
    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(47, 125, 251, 0.3);
    }
    .inline-actions {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        margin-top: 25px;
        padding-top: 20px;
        border-top: 1px solid var(--border);
    }
    @media (max-width: 992px) {
        .stats-grid, .tech-grid, .services-grid {
            grid-template-columns: repeat(2, 1fr);
        }
        .gallery-category .gallery-files {
            grid-template-columns: 1fr 1fr;
        }
    }
    @media (max-width: 576px) {
        .stats-grid, .tech-grid, .services-grid {
            grid-template-columns: 1fr;
        }
        .gallery-category .gallery-files {
            grid-template-columns: 1fr;
        }
    }
</style>

<div style="padding:20px;">
    <div style="background:var(--card-bg);border-radius:12px;padding:25px;">
        <h5 style="margin-bottom:25px;font-weight:700;">
            <i class="fa-solid fa-pen-to-square" style="color:var(--accent-2);margin-left:10px;"></i> ویرایش پروژه
        </h5>

        <form action="{{ route('projects.update', $project->id) }}" method="POST" enctype="multipart/form-data">
            @csrf
            @method('PUT')

            {{-- ===== اطلاعات اصلی ===== --}}
            <div class="form-section-title"><i class="fa-solid fa-circle-info"></i> اطلاعات اصلی پروژه</div>
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">عنوان پروژه <span style="color:#ef4444;">*</span></label>
                    <input type="text" name="title" value="{{ old('title', $project->title) }}" required class="form-control-custom">
                </div>
                <div class="col-md-6">
                    <label class="form-label">زیرعنوان</label>
                    <input type="text" name="subtitle" value="{{ old('subtitle', $project->subtitle) }}" class="form-control-custom" placeholder="مثلاً: افزونه‌ای برای حرفه‌ای شدن">
                </div>
                <div class="col-md-6">
                    <label class="form-label">دسته‌بندی</label>
                    <select name="cat_id" class="form-control-custom">
                        <option value="">انتخاب دسته‌بندی...</option>
                        @foreach($categories as $category)
                            <option value="{{ $category->id }}" {{ old('cat_id', $project->cat_id) == $category->id ? 'selected' : '' }}>{{ $category->name }}</option>
                        @endforeach
                    </select>
                </div>
                <div class="col-md-6">
                    <label class="form-label">نام کارفرما</label>
                    <input type="text" name="client_name" value="{{ old('client_name', $project->client_name) }}" class="form-control-custom" placeholder="مثلاً: شرکت بهین فرتاک">
                </div>
                <div class="col-md-6">
                    <label class="form-label">سمت کارفرما</label>
                    <input type="text" name="client_role" value="{{ old('client_role', $project->client_role) }}" class="form-control-custom" placeholder="مثلاً: مدیرعامل">
                </div>
                <div class="col-md-6">
                    <label class="form-label">سال اجرا</label>
                    <input type="text" name="launch_year" value="{{ old('launch_year', $project->launch_year) }}" class="form-control-custom" placeholder="مثلاً: ۱۴۰۲">
                </div>
                <div class="col-md-6">
                    <label class="form-label">مدت زمان</label>
                    <input type="text" name="duration" value="{{ old('duration', $project->duration) }}" class="form-control-custom" placeholder="مثلاً: ۳ سال">
                </div>
                <div class="col-md-6">
                    <label class="form-label">لینک پروژه</label>
                    <input type="url" name="project_link" value="{{ old('project_link', $project->project_link) }}" class="form-control-custom" placeholder="https://example.com">
                </div>
                <div class="col-12">
                    <label class="form-label">توضیح کوتاه (Brief)</label>
                    <textarea name="brief" rows="2" class="form-control-custom">{{ old('brief', $project->brief) }}</textarea>
                </div>
                <div class="col-12">
                    <label class="form-label">توضیحات کامل پروژه</label>
                    <textarea name="desc" rows="4" class="form-control-custom">{{ old('desc', $project->desc) }}</textarea>
                </div>
                <div class="col-12">
                    <label class="form-label">هدف پروژه</label>
                    <textarea name="project_goal" rows="3" class="form-control-custom" placeholder="هدف از انجام این پروژه چه بود؟">{{ old('project_goal', $project->project_goal) }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label" style="color:#f5a623;"><i class="fa-solid fa-triangle-exclamation"></i> چالش اصلی</label>
                    <textarea name="challenge" rows="4" class="form-control-custom" placeholder="چالش‌های اصلی پروژه را بنویسید...">{{ old('challenge', $project->challenge) }}</textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label" style="color:#00d1b2;"><i class="fa-solid fa-lightbulb"></i> راه‌حل ما</label>
                    <textarea name="solution" rows="4" class="form-control-custom" placeholder="راه‌حل‌های پیاده‌سازی‌شده را بنویسید...">{{ old('solution', $project->solution) }}</textarea>
                </div>
                <div class="col-12">
                    <label class="form-label">نقل قول از کارفرما (Testimonial)</label>
                    <textarea name="testimonial" rows="3" class="form-control-custom" placeholder="نظر کارفرما درباره پروژه...">{{ old('testimonial', $project->testimonial) }}</textarea>
                </div>
                <div class="col-12">
                    <label class="form-label">تصویر شاخص پروژه</label>
                    <div class="file-input-wrapper">
                        <input type="file" name="image" accept="image/*">
                        @if($project->image_url)
                            <div class="existing-image-preview">
                                <img src="{{ asset('storage/' . $project->image_url) }}" alt="تصویر فعلی">
                                <span style="font-size:0.85rem;color:var(--text-dimmer);align-self:center;">تصویر فعلی</span>
                            </div>
                        @endif
                    </div>
                </div>
            </div>

            {{-- ===== آمارها ===== --}}
            <div class="form-section-title"><i class="fa-solid fa-chart-simple"></i> آمارهای پروژه</div>
            <div class="stats-grid" id="statsContainer">
                @php
                    $stats = $project->stats->toArray();
                    $stats = array_pad($stats, 4, ['value' => '', 'label' => '']);
                @endphp
                @foreach($stats as $stat)
                    <div>
                        <input type="text" name="stats_value[]" value="{{ $stat['value'] ?? '' }}" class="form-control-custom" placeholder="مقدار (مثلاً ۴۵%)" style="margin-bottom:6px;">
                        <input type="text" name="stats_label[]" value="{{ $stat['label'] ?? '' }}" class="form-control-custom" placeholder="برچسب (مثلاً افزایش نرخ تبدیل)">
                    </div>
                @endforeach
            </div>

            {{-- ===== گالری تصاویر ===== --}}
            <div class="form-section-title"><i class="fa-solid fa-images"></i> گالری تصاویر</div>
            @php
                $galleryCategories = ['desktop' => 'دسکتاپ', 'mobile' => 'موبایل', 'key_pages' => 'صفحات کلیدی'];
                $existingGalleries = $project->galleries->groupBy('category');
            @endphp

            @foreach($galleryCategories as $catKey => $catLabel)
                <div class="gallery-category">
                    <span class="category-label"><i class="fa-regular fa-folder-open"></i> {{ $catLabel }}</span>
                    <div class="gallery-files">
                        @php
                            $images = $existingGalleries->get($catKey, collect())->toArray();
                            $images = array_pad($images, 3, ['image_url' => '']);
                        @endphp
                        @foreach($images as $idx => $img)
                            <div class="file-item">
                                <input type="file" name="gallery_images[{{ $catKey }}][]" accept="image/*">
                                @if(!empty($img['image_url']))
                                    <div style="display:flex;align-items:center;gap:6px;margin-top:4px;">
                                        <img src="{{ asset('storage/' . $img['image_url']) }}" class="preview-img" alt="تصویر موجود">
                                        <input type="hidden" name="gallery_existing[{{ $catKey }}][]" value="{{ $img['id'] ?? '' }}">
                                    </div>
                                @endif
                            </div>
                        @endforeach
                    </div>
                </div>
            @endforeach

            {{-- ===== تکنولوژی‌ها ===== --}}
            <div class="form-section-title"><i class="fa-solid fa-code"></i> تکنولوژی‌های استفاده‌شده</div>
            <div class="tech-grid">
                @php
                    $techs = $project->technologies->toArray();
                    $techs = array_pad($techs, 8, ['name' => '', 'icon' => '']);
                @endphp
                @foreach($techs as $tech)
                    <div>
                        <input type="text" name="tech_name[]" value="{{ $tech['name'] ?? '' }}" class="form-control-custom" placeholder="نام (مثلاً React)" style="margin-bottom:6px;">
                        <input type="text" name="tech_icon[]" value="{{ $tech['icon'] ?? '' }}" class="form-control-custom" placeholder="آیکون (مثلاً fa-brands fa-react)">
                    </div>
                @endforeach
            </div>

            {{-- ===== خدمات ===== --}}
            <div class="form-section-title"><i class="fa-solid fa-list-check"></i> خدمات ارائه‌شده در پروژه</div>
            <div class="services-grid">
                @php
                    $services = $project->services->toArray();
                    $services = array_pad($services, 5, ['name' => '']);
                @endphp
                @foreach($services as $service)
                    <div>
                        <input type="text" name="service_name[]" value="{{ $service['name'] ?? '' }}" class="form-control-custom" placeholder="نام خدمت (مثلاً طراحی UX/UI)">
                    </div>
                @endforeach
            </div>

            {{-- ===== دکمه‌ها ===== --}}
            <div class="inline-actions">
                <button type="submit" class="btn-primary">
                    <i class="fa-solid fa-save"></i> بروزرسانی پروژه
                </button>
                <a href="{{ route('projects.index') }}" class="btn-secondary">
                    <i class="fa-solid fa-arrow-right"></i> بازگشت به لیست
                </a>
            </div>

        </form>
    </div>
</div>
@endsection
</file>

<file path="index.blade.php">
// ذخیره آمارها
if ($request->has('stats_value')) {
    foreach ($request->stats_value as $index => $value) {
        if (!empty($value) && !empty($request->stats_label[$index])) {
            ProjectStat::create([
                'project_id' => $project->id,
                'value' => $value,
                'label' => $request->stats_label[$index],
            ]);
        }
    }
}

// ذخیره تکنولوژی‌ها
if ($request->has('tech_name')) {
    foreach ($request->tech_name as $index => $name) {
        if (!empty($name)) {
            ProjectTechnology::create([
                'project_id' => $project->id,
                'name' => $name,
                'icon' => $request->tech_icon[$index] ?? null,
                'order' => $index,
            ]);
        }
    }
}

// ذخیره خدمات
if ($request->has('service_name')) {
    foreach ($request->service_name as $index => $name) {
        if (!empty($name)) {
            ProjectService::create([
                'project_id' => $project->id,
                'name' => $name,
                'order' => $index,
            ]);
        }
    }
}

// ذخیره گالری
if ($request->hasFile('gallery_images')) {
    foreach ($request->file('gallery_images') as $category => $files) {
        foreach ($files as $file) {
            if ($file) {
                $path = $file->store('project_galleries', 'public');
                ProjectGallery::create([
                    'project_id' => $project->id,
                    'category' => $category,
                    'image_url' => $path,
                ]);
            }
        }
    }
}
</file>

</files>
